# Known R-parity gap signatures (the fuzzer's --baseline allowlist).
#
# Each line is a gap-CLASS signature: the diverging probe with numeric
# literals masked to N/F and quoted words to W, so many instances collapse
# to one class. Every entry is a REAL rlang gap already tracked in BUGS.md
# (unimplemented primitive, or a formatting difference vs reference R).
#
# Usage:  parity-fuzz --count 5000 --baseline tests/data/parity_fuzz_baseline.txt
# A run exits 0 while every divergence is in this list; a NEW gap-class
# (a regression, or newly reached surface) exits 1 so CI/you can triage it.
# Regenerate the raw set with a large sweep; add only triaged classes here.
#

(N + F) ^ N
apply(matrix(N:N, nrow = N), N, sum)
as.character(factor(c(W, W, W, W)))
as.character(factor(c(W, W, W)))
as.integer(factor(c(W, W, W, W)))
as.integer(factor(c(W, W, W)))
as.vector(table(c(c(N, N, N, N, N), c(N, N, N, N))))
as.vector(table(c(c(N, N, N, N, N), c(N, N, N))))
as.vector(table(c(c(N, N, N, N), c(N, N, N, N))))
as.vector(table(c(c(N, N, N, N), c(N, N, N))))
as.vector(table(c(c(N, N, N), c(N, N, N, N))))
as.vector(table(c(c(N, N, N), c(N, N, N))))
as.vector(table(factor(c(W, W, W, W))))
as.vector(table(factor(c(W, W, W))))
bitwAnd(NL, NL)
bitwNot(NL)
bitwOr(NL, NL)
bitwShiftL(NL, N)
bitwShiftR(NL, N)
bitwXor(NL, NL)
colSums(matrix(N:N, nrow = N))
diag(matrix(N:N, nrow = N))
duplicated(c(N, N, N, N, N))
duplicated(c(N, N, N, N))
f <- function(x, y = N) x + y; f(N)
format(F / F, nsmall = N)
formatC(F / F, digits = N, format = W)
formatC(N, width = N, flag = W)
is.element(N, c(N, N, N, N, N))
is.element(N, c(N, N, N, N))
is.element(N, c(N, N, N))
length(gregexpr(W, W)[[N]])
lengths(list(N:N, N:N))
levels(factor(c(W, W, W, W)))
levels(factor(c(W, W, W)))
mapply(function(a, b) a + b, N:N, N:N)
match(c(N, N, N, N, N), c(N, N, N, N, N))
match(c(N, N, N, N, N), c(N, N, N, N))
match(c(N, N, N, N, N), c(N, N, N))
match(c(N, N, N, N), c(N, N, N, N, N))
match(c(N, N, N, N), c(N, N, N, N))
match(c(N, N, N, N), c(N, N, N))
match(c(N, N, N), c(N, N, N, N, N))
matrix(N:N, nrow = N, byrow = TRUE)
matrix(N:N, nrow = N) %*% diag(N)
N > N
nlevels(factor(c(W, W, W, W)))
nlevels(factor(c(W, W, W)))
prettyNum(N, big.mark = W)
rank(c(N, N, N, N, N))
rank(c(N, N, N, N))
rank(c(N, N, N))
Reduce(function(a, b) a * b, N:N, accumulate = TRUE)
regmatches(W, regexpr(W, W))
rowSums(matrix(N:N, nrow = N))
signif(F * F, N)
sprintf(W, F)
sprintf(W, N)
storage.mode(NL)
strsplit(W, W)
table(factor(c(W, W, W, W)))
table(factor(c(W, W, W)))
xor(N > N, N > N)
